stage1:
write a native client use swift on macOS and c# on windows, both of them can work as a server or client
using websocket to sync data
it can captures system clipboard and sync to another, override the clipboard of another system
works in menuBar macOS or task system tray on windows only
allows config the server ip and port, and config as client or server, shows a status
accept text only

stage2:
clipboard history, accept image and file
